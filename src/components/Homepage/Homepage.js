import React from 'react';
import PropTypes from 'prop-types';


const Homepage = () => (
  <div>
    Homepage Component
  </div>
);

Homepage.propTypes = {};

Homepage.defaultProps = {};

export default Homepage;
